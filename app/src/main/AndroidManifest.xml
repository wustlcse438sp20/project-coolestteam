<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.EmployerUI.MatchesEmployerActivity"></activity>
        <activity android:name=".Activities.EmployerUI.ProfileEmployerActivity" />
        <activity android:name=".Activities.EmployerUI.CreateJobActivity" />
        <activity android:name=".Activities.EmployeeUI.MatchesEmployeeActivity" />
        <activity android:name=".Activities.EmployeeUI.ProfileEmployeeActivity" />
        <activity android:name=".Activities.EmployerUI.HomeEmployerActivity" />
        <activity android:name=".Activities.EmployerUI.PostHomeEmployerActivity"/>
        <activity android:name=".Activities.MainActivity"/>

        <activity android:name=".Activities.LoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Activities.SignupActivity" />
        <activity android:name=".Activities.EmployeeProfileActivity" />
        <activity android:name=".Activities.EmployerProfileActivity" />
        <activity android:name=".Activities.CreateJobPostingActivity" />
        <activity android:name=".Activities.EmployeeUI.HomeEmployeeActivity" />
    </application>

</manifest>